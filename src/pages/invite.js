import React, { Component } from "react"

import Layout from "../components/Layout/Layout"

export default class Invite extends Component {
  render() {
    return (
      <div>
        <Layout>
          <h1>Hello Invite, old friend.</h1>
        </Layout>
      </div>
    )
  }
}
